server:
  port: ${MS_APPOINTMENTS_PORT:0}
spring:
  application:
    name: ms-appointments
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${MS_APPOINTMENTS_DB:db.ctd.academy:3306/pi21023c02_GRUPO6}?useSSL=false&useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
    username: ${MS_APPOINTMENTS_DB_USERNAME:pi21023c02_GRUPO6}
    password: ${MS_APPOINTMENTS_DB_PASSWORD:iguu3Ohg}
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  #server:
  # error:
  #  include-message: always

eureka:
  client:
    service-url:
      defaultZone: ${MS_APPOINTMENTS_SERVICE_URL:http://localhost:8761/eureka/}
  instance:
    hostname: ${MS_APPOINTMENTS_HOSTNAME:localhost}
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
